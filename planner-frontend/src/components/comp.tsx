import React, { useState } from 'react';

interface Props {
    className?: string;
}

export const Comp: React.FC<Props> = ({ className }) => {

    const [ ] = useState()

  return (
    <div className={className}></div>
  );
};